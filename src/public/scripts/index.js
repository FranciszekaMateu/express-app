/*const socket = io()
socket.on("recibido", () => {
    console.log("mensaje recibido");
  });
 const send = (data,event) => socket.emit(event,data)
const buttonSend = document.getElementById("buttonSend")
const buttonLogIn = document.getElementById("buttonLogIn")
function sendsocket(event,socket,id)
{
    const element = document.getElementById(id)
    sendsocket(socket,element.value)
    event.preventDefault()
}
buttonSend.addEventListener(click,(event) => sendsocket(event,"message","message"))
buttonLogIn.addEventListener(click,(event) => sendsocket(event,"logeado","email"))  */
